<% layout("/layouts/boilerplate.ejs") %>
<body>

    <h3 class="mt-3">All Listings</h3>
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3 g-0">
  <% for (let listing of allListings) { %>
    <div class="col mb-4 d-flex justify-content-center">
      <a href="/listings/<%= listing._id %>" class="listing-link w-100" style="text-decoration: none;">
        <div class="card listing-card" style="width: 20rem;">
          <img src="<%= listing.image.url %>" class="card-img-top" alt="listing_image" style="height: 20rem;">
          <div class="card-img-overlay"></div>
          <div class="card-body">
            <h5 class="card-title"><%= listing.title %></h5>
            <p class="card-text">&#x20b9; <%= listing.price.toLocaleString("en-IN") %> /night</p>
          </div>
        </div>
      </a>
    </div>
  <% } %>
</div>
    


    
</body>

